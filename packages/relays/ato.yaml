requires-atopile: "^0.9.0"

paths:
  src: .
  layout: ./layouts

builds:
  default:
    entry: relays.ato:PowerRelay
  test:
    entry: relays.ato:Test

package:
  identifier: atopile/relays
  repository: https://github.com/atopile/packages
  version: "0.1.3"
  authors:
    - name: atopile
      email: hi@atopile.io
  summary: DPDT relay with logic level driver circuit and LED indicator
  license: MIT
