requires-atopile: "^0.10.0"

paths:
  src: "."
  layout: ./layouts

builds:
  default:
    entry: icm20948.ato:ICM20948_driver
    hide_designators: true
    exclude_checks: ["PCB.requires_drc_check"]
  example:
    entry: icm20948.ato:Example
    hide_designators: true
    exclude_checks: ["PCB.requires_drc_check"]

